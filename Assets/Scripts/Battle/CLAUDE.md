# Battle 模块指南

这个目录负责可复用的神器战斗规则和数据结构。

## 职责

- 神器目录数据和运行时状态。
- 自动释放计算和战斗报告。
- 目标选择和显示文本辅助。
- 备战装载队列状态。

## 不放这里

- 场景对象查找、相机、HUD 控件、输入回调、场景流程切换属于 `Assets/Scripts/Prototype/`。
- 修改战斗领域行为通常不应该需要手改 Unity 场景 YAML。

## 验证方式

- 优先围绕被修改的类和调用方做聚焦检查。
- 如果改了行为，同时检查消费该 API 的原型控制器或 HUD。
